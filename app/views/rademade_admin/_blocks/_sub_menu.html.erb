<ul class="submenu"><%
    menu_item[:sub].each do |sub_item|
        concat render @template_service.sub_menu_link_block, {
            :uri => admin_list_uri(sub_item[:model_info]),
            :title => sub_item[:model_info].item_name
        }
        concat render @template_service.sub_menu_block, {
            :menu_item => sub_item
        }
    end
%></ul>