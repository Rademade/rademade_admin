#class @ImagePreview extends Backbone.View
#
#  @initPlugin : () ->
#    $('[data-image-preview]').click (e) ->
##      model = new
#
#  @initPlugin : () =>
#    @init()
#
#$ ->
#  $(document).on 'page:load ready init-plugins', ImagePreview.initPlugin